{% extends 'base.html.twig' %} {% block title %}Hello Voiture{% endblock %} {%
block body %}
<div class="card" style="width: 18rem">
  {% for list in lists %}
  <div class="card-body">
    <h5 class="card-title">{{ list.dateLocation | date("d/m/y")}}</h5>
    <p class="card-text"></p>
  </div>
  <ul class="list-group list-group-flush">
    <li class="list-group-item">Nombre de km :{{ list.nbJourLocation }}</li>
    <li class="list-group-item">
     Prix : {{list.prixLocation}} €
    </li>
  </ul>

  {% endfor %}
</div>
{% endblock %}